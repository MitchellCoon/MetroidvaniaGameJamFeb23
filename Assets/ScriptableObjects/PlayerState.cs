using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerState : ScriptableObject
{

    // [SerializeField] MovementOverride _defaultOverride;
    // [SerializeField] float _initialHP = 100f;

    // MovementOverride _currentOverride;

    // // public getter
    // MovementOverride currentOverride => _currentOverride;

    // public void Init() {
    //     _currentOverride = _defaultOverride;
    // }

    // public void AcquireOverride(MovementOverride _incoming) {
    //     _currentOverride = incoming;
    // }
}
